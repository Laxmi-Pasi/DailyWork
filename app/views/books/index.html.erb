<%= #javascript_include_tag "main" 
%>
<%= stylesheet_link_tag "main" %>
<% content_for :head do %>
  <title>books</title>
<% end %>
<h1>Listing Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.name %></td>
        <td><%= book.price %></td>
        <td><%= link_to "Show", book %></td>
        <td><%= link_to "Edit", edit_book_path(book) %></td>
        <td><%= link_to "Destroy", book, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "New book", new_book_path %><br/>
<%= image_tag "img1.png", {height: 100, width: 100} %>
<%= #video_tag "movie.ogg" 
%>

